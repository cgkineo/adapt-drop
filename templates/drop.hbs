<button class="{{#if title}}btn-text{{else}}btn-icon{{/if}} drop__btn{{#if _classes}} {{_classes}}{{/if}} js-toggle-view" aria-expanded="false"{{#if ariaLabel}} aria-label="{{ariaLabel}}"{{/if}}>

  <div class="drop__btn-icon">
    <div class="icon"></div>
  </div>

  {{#if title}}
  <div class="drop__btn-title">
    <div class="drop__btn-title-inner">
      {{{title}}}
    </div>
  </div>
  {{/if}}

</button>
